<div class="span3">

    {{ render(controller("EcommerceBundle:Panier:menu")) }}
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        {% include('@Utilisateurs/modulesUsed/utilisateursConnecte.html.twig') %}
    {% else %}
        {% include('@Utilisateurs/modulesUsed/utilisateurs.html.twig') %}
    {% endif %}
    <div class="well">
        <ul class="nav nav-list">
            <li class="nav-header">Nos produits</li>
            {{ render(controller("EcommerceBundle:Categories:menu")) }}
        </ul>
    </div>
</div>